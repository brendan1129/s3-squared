# $s3^2$
S9 - Serverless distributed consumer cloud file storage solution leveraging AWS S3, API Gateway, IAM, and Lambda.

This project leverages AWS SAM to automatically create the Lambda function, configure API Gateway with a public URL, and hook up the correct IAM permissions.
